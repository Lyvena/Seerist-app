# Seerist web app — public client configuration.
# Both values are safe to expose in the browser bundle (the anon key is a
# public credential). Server-side secrets (ik_ API key, Composio, Creem,
# OpenHands) live in InsForge project secrets, never here.
VITE_INSFORGE_URL=https://si9f4zab.eu-central.insforge.app
VITE_INSFORGE_ANON_KEY=anon_7ce1c8516e22d8090c0d06f724319b00b352bd6357f783756ea8c1871d4993a1
